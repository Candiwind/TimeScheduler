【APP问题，先跳过】
1. app和html应当能同步数据
2. app的大任务板块在添加新的大任务后排版会出问题，请修正

────────────────────────────────────────

【V1.1 网页版问题，已修正】

── 基础功能 ──
 1. ✅ Claude Code规划改成AI规划
 2. ✅ 象限内部任务顺序支持拖动修改
 3. ✅ 日期输入优化（天数快速输入）
 4. ✅ 待办任务池任务可拖入四象限
 5. ✅ 任务推迟后备注推迟数/总任务数
 6. ✅ 标记今天额外完成的任务
 7. ✅ 加入最近的完成情况统计
 8. ✅ 任务占比自动计算（I>II>III>IV，合计100%）
 9. ✅ 下划线功能，划出重要信息片段
10. ✅ 下划线加粗改成highlight
11. ✅ 头部两栏合并，缓存/统计/日期放外面，其余折叠
12. ✅ highlight逻辑：鼠标划选+Ctrl+Q或右键
13. ✅ highlight在任务完成后失效
14. ✅ 时间段图标（早晨/上午/中午/下午/傍晚/晚上/空）
15. ✅ highlight功能恢复
16. ✅ 任务块内子任务完成度自动计算
17. ✅ 大任务完成后四象限浅色渐变
18. ✅ 日报生成功能（预览+下载）
19. ✅ 页面可伸缩性优化（同行象限等高）
20. ✅ 导入缓存不修改现有任务（合并模式）
21. ✅ 周计划、月计划任务池
22. ✅ 大任务块内子任务排序+拖出块
23. ✅ 大任务块跨象限迁移

── UI优化 ──
24. ✅ 待办/周/月合并为Tab切换面板（节省2/3纵向空间）
25. ✅ Hint Bar可关闭（localStorage记忆）
26. ✅ 大任务面板(紫)与周计划(蓝)颜色区分
27. ✅ 统计栏改为紧凑行内样式
28. ✅ 计划池空时折叠、有任务自动展开
29. ✅ +任务块按钮缩小为📦图标
30. ✅ 象限footer有任务时自动缩小

── UI迭代（8轮）──
31. ✅ R1 全局美化：背景渐变、selection色、滚动条、reduced-motion、阴影层级、聚焦环
32. ✅ R2 象限质感：左侧彩色边框、hover阴影提升、头部渐变更鲜明
33. ✅ R3 任务交互：hover位移动画、完成态透明度、拖拽反馈、进度条动画
34. ✅ R4 任务块重构：边框轻量化、hover高亮、内容分隔线、子任务缩进、空状态图标
35. ✅ R5 头部精简：padding缩小、字号下调、shadow-sm、紧凑间距
36. ✅ R6 面板美化：装饰条、backdrop-filter模糊、Toast装饰条
37. ✅ R7 暗色模式：bg加深、surface层次、accent提亮、象限头四色独立定义
38. ✅ R8 移动端：contain性能优化、will-change、三档响应式、触摸目标≥26px

── 工程 ──
39. ✅ 上传GitHub（Candiwind/TimeScheduler）
40. ✅ Claude Code权限配置（settings.local.json）

────────────────────────────────────────

【V1.2 网页版问题，已修正】

── 核心逻辑修正 ──
41. ✅ 加权计算逻辑修正：所有任务权重均等，全部完成后加权总和=100%
    单项权重随任务数量增减自动调整（权重=100%÷任务总数）
    涉及文件：render.js / stats-ui.js / daily-report.js
    移除旧的象限固定权重（I×0.35 + II×0.3 + III×0.2 + IV×0.15）
    统计面板中"加权X%"改为"任务占比X%"，单任务占比统一为100/N%

── 日报增强 ──
42. ✅ 日报导出内容包含待办池（含周计划/月计划）任务
    涉及文件：daily-report.js

── 功能增强（7项）──
43. ✅ 整体完成情况移至网页首行：header中新增完成情况badge（✅ done/total · rate%）
    涉及文件：index.html / render.js / style.css
44. ✅ 象限右上角信息从任务总数改为完成情况 a/b c%
    涉及文件：render.js / style.css
45. ✅ 大任务面板每日任务量提醒：今日池任务>3时显示警告
    涉及文件：bigtask.js / style.css
46. ✅ 子任务阶段拆分：支持将子任务拆分为多个阶段，阶段与普通子任务等权重
    每个阶段独立checkbox，自动同步父级完成状态
    涉及文件：render.js / quadrant-ops.js / style.css
47. ✅ 大任务内子任务任意拖拽：支持在里程碑内排序、跨里程碑移动、拖入象限
    涉及文件：bigtask.js / drag.js / style.css
48. ✅ 新增"依循"模块：设置起止日期，添加1~5条（建议≤3）依循
    涉及文件：store.js / index.html / render.js / app.js / style.css
49. ✅ 模块默认折叠：大任务/计划池/原则面板默认折叠，子操作不导致已展开面板关闭
    涉及文件：index.html / bigtask.js / render.js
50. ✅ 去除重复的第二行stats bar：整体完成情况仅保留首行header badge
    涉及文件：index.html / render.js / style.css
51. ✅ 统计面板新增分时段完成情况：早晨+上午/中午+下午/傍晚+晚上三组
    涉及文件：stats-ui.js
52. ✅ 划分为阶段之后，原来小任务后面的各个标签，除了“划分阶段”这个功能之外，其他的
  全都要转移到阶段那里

────────────────────────────────────────

【V1.3 网页版问题，已修正】
1. ✅ 大任务规划栏那里整体加一个”完成”选项，可以自己点击
2. ✅ 任务推迟数量每天清理重新统计
3. ✅ 网页第一行，统计结果除了第一样整体统计情况之外，其他数据字号小一点
4. ✅ 补全象限任务块内子任务按时间段排序（render.js createTaskBlockElement）
5. ✅ 象限内小任务添加按钮重新设计为小按钮，位于右上角统计badge左侧
6. ✅ 修复大任务自动加载进象限的小任务拖动到其他象限后原象限任务不消失的问题
  （moveTaskIntoBlock/moveSubtaskOut/moveSubtaskToAfter 保留 bigTaskRef；
   addPoolTaskToQuadrant/moveBigtaskSubToQuadrant 也检查块内子任务的 bigTaskRef，
   避免 migrateBigTaskSubtasks 重复导入）
7. ✅ handleStageDrop 修复：不再无条件阻止事件冒泡，避免拖拽任务到阶段上时被吞掉
8. ✅ migrateBigTaskSubtasks 新增去重逻辑：拖拽后若象限II残留同 bigTaskRef 的旧任务，自动清理
9. ✅ 修复删除大任务子任务后仍被重新导入的问题：deleteTaskDirect/deleteTask/deleteBlock/
   deleteSubTask 删除时自动清除 bigTaskSubtask.plannedDate，阻止 migrateBigTaskSubtasks 再次导入
10. ✅ 划分阶段后添加阶段按钮移至任务右上角：addStageBtn 改为右上角小按钮（margin-left:auto），
    splitBtn 在已有阶段时隐藏，按钮通过 insertBefore 置于 delBtn 之前
11. ✅ 阶段支持手动拖拽排序：移除渲染时的 timeSlot 强制排序，改为按完成状态排序
   （未完成在前、已完成在后），同一状态组内保留手动拖拽顺序。
   拖拽基础设施已完备（handleStageDrop → reorderStages/reorderTaskStages），
   仅需修改 createTaskElement / createSubTaskElement 中的 stages.sort 逻辑
   涉及文件：render.js


────────────────────────────────────────

【V1.4 网页版问题，已修正】
1. ✅ 大任务子任务导入象限时连带阶段：当大任务规划的小任务（含阶段）通过自动迁移、
   手动导入、池拖拽、面板拖拽等方式导入象限时，其阶段也会被一并复制（新ID避免冲突）。
   新增 copyBigSubtaskStages 共享工具函数，
   涉及文件：store.js / future.js / bigtask.js / drag.js
2. ✅ 修复任务/子任务/阶段排序逻辑：按完成状态分组（未完成在前、已完成在后），
   同一组内按 timeSlot 排序，保留组内手动拖拽顺序（稳定排序）。
   涉及文件：render.js（renderQuadrant / createTaskBlockElement / createTaskElement / createSubTaskElement）
3. ✅ 当天数据不自动加入缓存库：引入显式缓存索引（quadrant_cached_dates_index），
   只有用户点击"缓存"的日期才出现在导入缓存对话框中。
   向后兼容：首次加载时自动将现有数据种子化到缓存索引。
   涉及文件：store.js / cache-ui.js / app.js
4. ✅ 按完成时间查看象限待办（时段视图）：新增视图切换按钮，可在象限视图和时段视图间切换。
   时段视图按 timeSlot 分组显示所有象限任务，组内按 I→II→III→IV 象限排布。
   两种视图共享同一数据模型，修改自动同步；视图偏好保存到 localStorage。
   涉及文件：index.html / render.js / app.js / style.css
5. ✅ 优化第一行 header 渲染尺寸：减小标题字号(15px)、按钮字号(11px)和padding(4px 8px)、
   图标按钮尺寸(28px)、完成badge字号(11px)、时段badge字号(9px)；
   缩短按钮文字（缓存、导入、📈、📋），确保任务数达三位数时第一行不换行。
   涉及文件：style.css / index.html
6. ✅ 修复时间视图切换回象限视图失效：renderTimeView 原先用 innerHTML 清空 .quadrant-grid
   导致4个象限容器被销毁，切换回象限视图时 renderQuadrant 找不到容器。改为隐藏/显示机制：
   时间视图隐藏 .quadrant 元素、显示 #timeview-container；象限视图恢复显示。
   涉及文件：index.html / render.js
7. ✅ 时间视图任务显示象限背景色：任务元素按所属象限渲染浅色背景
   （I红/II蓝/III黄/IV绿），已完成任务保留背景色仅文字划线，暗色模式适配。
   涉及文件：render.js / style.css
8. ✅ 时间视图子任务和阶段按各自时段分布：当任务含阶段或任务块含子任务时，
   每个阶段/子任务按其自身的 timeSlot 分散到对应时段section中独立显示，
   保留父任务引用标签（↳ 父任务名），父任务本身仍保留在原时段section中。
   支持嵌套：块→子任务→阶段三级均可按各自时段分布。
   涉及文件：render.js / style.css
9. ✅ 修复时间视图删除操作：父任务/块改为紧凑卡片显示（不重复渲染嵌套子项），
   删除时弹出含子项数量的确认提示"确定删除XX及其N个子项？"；
   子项（阶段/子任务）可独立删除，删除后自动重新渲染；
   支持撤销删除（Toast undo）。
   涉及文件：render.js / style.css
10. ✅ 时间视图改为智能网格布局：根据有任务的时段数量自动调整列数
   （1时段→1列, 2时段→2列, 3时段→3列, 4时段→2×2, 5时段→3+2, 6时段→3×2），
   替代原先垂直堆叠布局，空间利用率更高。
   响应式由JS动态适配（≤600px→max1列, ≤1000px→max2列）。
   涉及文件：style.css / render.js
11. ✅ 消除"无时段要求"分类：时间视图移除"无时段要求"section，所有任务/子任务/阶段
   默认按当前时间自动分配时段（getDefaultTimeSlot：5-8点→早晨, 8-12→上午,
   12-14→中午, 14-17→下午, 17-20→傍晚, 其他→晚上）。
   修复全部16处创建路径（addTask/addSubTask/addStage/splitStages/migrateBigTask/
   importBigSubtask/addPoolTask/moveBigtaskSub等），确保新条目始终含timeSlot。
   涉及文件：timeslot.js / render.js / quadrant-ops.js / store.js / future.js / bigtask.js / drag.js
12. ✅ 时间视图各时段块添加滚动条：.timeview-items 设置 max-height: 48vh + overflow-y: auto，
   与象限视图一致，内容超出时各时段块独立滚动。移动端 max-height: 30vh。
   涉及文件：style.css
13. ✅ 时间视图重构为象限式分组布局：移除紧凑卡片（createCompactParentEl）和父子引用标签
   （wrapChildEl），改为父任务/任务块在各子项时段以分组标题（createParentHeaderEl）出现，
   子项按象限视图风格完整展示（含全部操作按钮）。大任务显示🎯标题。排序改为象限优先
   （I→II→III→IV），同象限内分组标题→子项→独立任务。子任务阶段保留块→子任务两级引用。
   涉及文件：render.js / style.css
14. ✅ 时间视图禁止添加新项：隐藏 .add-stage-btn / .split-stages-btn / .add-subtask-btn，
   仅保留编辑(文字/时段/完成状态)、删除、推迟等操作，避免在时段视图中修改任务结构。
   涉及文件：style.css


────────────────────────────────────────

【V1.5 网页版问题，已修正】
1. ✅ 修复子任务阶段分布bug：flattenChildren 中 subtask-stage 分布后 parentChildSlots
   错误使用 tSlot（子任务时段）而非 ssSlot（阶段时段），导致阶段所在时段缺少父标题卡片。
   现改为 ssSlot，确保大任务内小任务的阶段正确按时间分布到各自时段并显示父标题。
   涉及文件：render.js
2. ✅ 修复时间视图子任务阶段重复显示：含阶段的子任务在自身时段渲染时，createSubTaskElement
   会附带嵌套的 .subtask-stages 容器，而这些阶段又作为 subtask-stage 条目分布到各自时段，
   导致同一阶段在不同板块出现两次。新增 opts.skipStages 参数，时间视图渲染分布式子任务时
   跳过嵌套阶段容器（仅保留子任务行），完成状态同步逻辑前移保证正确。
   涉及文件：render.js
   验证：test/test-timeview-distribution.js（4场景23断言全通过，覆盖无重复性）
3. ✅ 调整时间视图排布为"一行两个时间段"：6个时段全有任务时由 3×2（3列2行）改为 2×3
   （一行两个、共3大行），与象限视图 2×2 风格一致。统一原则：1时段→1列，2+时段→2列/行
   （3时段→2+1，4时段→2×2，5时段→2+2+1，6时段→2×3）。移动端(≤600px)仍强制单列。
   涉及文件：render.js / style.css
   验证：test/test-timeview-columns.js（桌面列数/移动端单列/行数推导全覆盖）
4. ✅ 时间视图改为分组嵌套排版，清晰展示归属关系：原先每个时段内是扁平列表（所有父标题→
   所有子项→独立项），子任务/阶段脱离父级、看不出归属。重构为 buildSlotUnits+appendUnit：
   每个父标题(📦块/📋带阶段任务，🎯标注大任务)紧接其"属于当前时段"的子项，按象限视图风格
   缩进——小任务缩进1级、小任务下的阶段缩进2级、带阶段任务的阶段缩进1级。若某阶段的子任务
   在另一时段，则用"↳ 子任务名"孤立标签标注其归属。删除 createCompactParentEl/wrapChildEl/
   sortGroup 等旧扁平渲染代码及对应死CSS。createParentHeaderEl 改为内部统计全部子项数(徽标+
   删除确认更准确)。分布逻辑(flattenChildren)不变，故无重复/无遗漏/时段正确的不变量保持。
   涉及文件：render.js / style.css
   验证：test/test-timeview-grouping.js（5场景17断言：嵌套层级/孤立标签/多父不交叉/独立项按象限穿插）
5. ✅ 大任务完成后更新现存数量+显示逾期完成天数：
   (1) 现存数量改为统计"未完成"大任务（progress<100）。面板标题 #bigTaskCount 由 bigTasks.length
   改为 countActiveBigTasks(bigTasks)；addBigTask 的 MAX_BIG_TASKS(=5) 上限也改用 countActiveBigTasks
   判定——已完成的大任务不再占用名额，完成一个即可释放一个活跃名额。新增 countActiveBigTasks 辅助。
   (2) 进度首次达到 100% 时记录 completedDate（本地日期），用于逾期统计。两条完成路径均覆盖：
   自动路径（子任务/阶段勾选、AI导入、拖拽等触发 recalcBigTaskProgress）在函数内统一盖章/清除；
   手动路径（完成按钮）在 updateBigTask 的 updates 里同步设置/清除。回退到未完成时清除 completedDate，
   再次完成则重新盖章（不留陈旧日期）；已有 completedDate 不会被后续 recalc 覆盖。
   卡片渲染：progress>=100 时若 bigTaskOverdueDays>0 显示"逾期完成 N 天"(红色 urgent)，否则显示"✅ 已完成"。
   新增 todayLocalDateStr / bigTaskOverdueDays 辅助。
   涉及文件：store.js / bigtask.js
   验证：test/test-bigtask-completion.js（6场景28断言：活跃计数/逾期天数/recalc盖章与回退/手动按钮payload/MAX名额释放）
6. ✅ 大任务完成后自动归档至缓存库：
   (1) saveBigTasks 自动检测 progress>=100 的任务，移入 BIG_TASK_CACHE_KEY 缓存库并清出活跃列表。
   已完成的旧数据在 initApp 时 silent migration 一次归档（不弹 toast）。
   (2) suppressAutoArchive 标志位防止恢复后立即重新归档；progress 降至 <100 时自动清除标志位。
   (3) 大任务面板底部新增"大任务缓存库"可折叠区域，展示已归档任务的截止/完成日期及逾期天数，
   支持恢复（↩）回活跃列表和永久删除。
   (4) render.js getBigTaskName 扩展为同时查询活跃列表和缓存库，保证时间视图中已归档任务的 🎯 名称仍能解析。
   涉及文件：store.js / bigtask.js / index.html / css/style.css / render.js / app.js
   验证：test/test-bigtask-cache.js（9场景30断言：归档/去重/suppress/恢复/删除/端到端生命周期）
7. ✅ CSS 缺陷修复与触摸目标优化：
   (1) 修复 var(--primary) 和 var(--text4) 未定义导致时间视图父标题左边框和大任务名颜色失效。
   (2) 移除常驻 will-change:transform 反模式（所有 task-item/subtask-item/task-block），释放合成层内存。
   (3) 删除无效 CSS draggable:true 声明和 .btn-add-block 死规则。
   (4) 桌面端小按钮（delete/defer/extra/quadrant-add/split-stages/time-slot）22→24px / 18→22px，
   移动端统一 30px 且补全被遗漏的象限添加/拆分/时段选择按钮。
8. ✅ 重复逻辑抽取与死代码清理：
   (1) 新增 walkLeafItems 统一遍历函数（util.js），统一块→子任务→阶段三级嵌套的"叶子项"遍历逻辑。
   calcQuadrantCompletion / calcTimeSlotCompletion / countAllTasks 三处改用 walkLeafItems，
   从 ~90 行重复遍历缩减为各自 4-8 行。以后统计口径改动只需改一处。
   (2) 合并 showTimeSlotPickerCustom 与 showTimeSlotPicker 为统一的 showTimeSlotPicker(el, qk, tid, bid, opts)。
   (3) 删除 app.js 中未引用死代码 rebindDragEvents / setupDragDrop（拖拽事件已在 create*Element 中直接绑定）。
9. ✅ 数据安全加固——滚动备份 + schema 版本号 + 配额检测：
   (1) 备份从同步镜像改为滚动保留最近 3 个快照（_backup / _backup_1 / _backup_2），
   逻辑错误导致的损坏不会同时覆盖所有备份——检测到越早有更多恢复机会。
   损坏恢复时按新旧顺序依次尝试所有备份层。
   (2) 新增 SCHEMA_VERSION / SCHEMA_VERSION_KEY，为未来数据结构变更提供迁移检测锚点。
   (3) 存储空间不足时估算使用率并给出具体清理建议（导出JSON后清理历史日期），替代通用提示。
10. ✅ 键盘可访问性——为所有交互文本/卡片元素添加 :focus-visible 样式：
	   双击编辑文本（task-text / principle-text / block-name / planpool-item-text 等）、
	   可折叠面板标题（principles / bigtask / planpool / cache header）、可展开卡片（bigtask-card / milestone）
	   统一设置 outline: 2px solid var(--accent)，键盘用户可清晰感知焦点位置。
11. ✅ 修复大任务缓存库空活跃列表时无法渲染：
	   renderBigTaskPanel 在 bigTasks.length===0 时 early return 跳过 renderBigTaskCache()
	   和 flushArchiveToasts()，导致缓存数据虽写入 localStorage 但 UI 从未展示。
	   修复：在 early return 分支中补充调用 renderBigTaskCache() + flushArchiveToasts()，
	   确保缓存库渲染独立于活跃任务列表。
	   验证：test/test-bigtask-cache.js（10场景37断言全通过，新增 Test10 覆盖空活跃列表场景）
	   12. ✅ 修复夜间模式 Highlight 样式：mark 元素背景从渐变（仅覆盖底部40%）改为纯色覆盖完整字体高度；
	    夜间模式下 font color 从浏览器默认黑色改为 #e8e8e8（亮色），确保暗色背景上可读。
	    涉及文件：css/style.css
	13. ✅ PWA 增强——manifest.json 补全：新增 scope、display_override、categories、screenshots、
	    shortcuts、purpose 等字段，提升华为/QQ/夸克等浏览器添加到桌面兼容性。
	    涉及文件：manifest.json
	14. ✅ Service Worker 重写——增强缓存策略：预缓存所有核心资源 + 逐个失败容错、激活时清理旧版缓存、
	    强制 skipWaiting+claim 立即接管、HTML 请求后台 Stale-While-Revalidate 更新、
	    离线时返回中文离线提示页、注册时监听 updatefound 提示版本更新。
	    涉及文件：service-worker.js / index.html
	15. ✅ PWA meta 标签补全：新增 X-UA-Compatible、viewport-fit=cover、shrink-to-fit=no、
	    apple-mobile-web-app-status-bar-style、mobile-web-app-capable、msapplication-TileColor、
	    format-detection、HandheldFriendly、application-name、apple-touch-icon 多尺寸链接。
	    涉及文件：index.html
	16. ✅ 手机端源码编辑器：支持 HTML/CSS/JS 三 tab 编辑、👁预览/✏编辑一键切换（iframe 预览）、
	    localStorage 自动保存（刷新不丢失）、📥导出完整 HTML 文件下载、🔄重置为原始代码。
	    编辑器全屏适配手机、软键盘弹出时 textarea 自动收缩(max-height:50vh)、
	    按钮触摸目标≥36px、暗色模式适配。
	    涉及文件：js/source-editor.js（新增）/ index.html / js/app.js / css/style.css
	17. ✅ 移动端触摸目标从 26px 提升至 48px（符合华为手机多浏览器适配要求）：
	    任务行 min-height:48px、操作按钮 48×48px、checkbox 22px、普通按钮 min-height:48px、
	    小按钮 min-height:36px、图标按钮 48×48px、Tab 按钮 min-height:48px。
	    task-item/subtask-item 字体适当放大（14px/13px → 提升可读性）。
	    涉及文件：css/style.css


────────────────────────────────────────

【网页版问题，待修正】
18. ✅ Gist 云同步修复——手机端免 Token 拉取 + 自动刷新视图：
   (1) pullFromGist 自动附带 Token（如果已配置），私密 Gist 也能读取。
   (2) 新增公开 Gist 免 Token 模式：只需输入 Gist ID 即可拉取数据，手机端最简配置。
       setupGistSync 先尝试无 Token 访问，若 Gist 公开则直接进入"仅拉取模式"；
       若失败再提示输入 Token 切换到"拉取+推送"完整模式。
   (3) pullFromGist 导入数据后自动调用 refreshAllViews 刷新全部视图（象限/大任务/计划池/缓存等），
       手机端首次设置后无需手动刷新。
   (4) 推送按钮在仅拉取模式下 disabled 并标注"(需Token)"，点击时给出明确提示。
   (5) SW 缓存版本升至 v3，确保更新后的 cloud-sync.js 能被客户端拉取。
   涉及文件：js/cloud-sync.js / service-worker.js
19. ✅ 手机端竖屏条目布局优化——解决一行放不下 checkbox+文本+5个按钮导致挤压重叠：
   (1) task-item / subtask-item 在 ≤600px 下 flex-wrap，task-left（checkbox+文本）独占第一行，
       操作按钮（高亮/额外/推迟/时段/删除/拆分）折到第二行均分宽度（flex:1 1 0, min-width:44px），
       不溢出、触摸目标充足。
   (2) 任务文本在手机端允许换行（white-space:normal + word-break），完整显示不再被截断。
   (3) 第一行与第二行之间用虚线分隔，视觉层次清晰。
   (4) block-header / subtask-stage-item 同样允许 flex-wrap，防止长标题+控件溢出。
   涉及文件：css/style.css
20. ✅ 云同步方向明确化——对话框状态栏显示当前同步方向：
   - Gist + Token → ⬆️⬇️ 双向同步（本端改动 3 秒后自动推送）
   - Gist 仅 ID  → ⬇️ 仅拉取（本端改动不会上传，需填 Token 才能双向）
   - 百度网盘    → 💻 电脑端自动写入网盘
   手机端修改数据可同步：配置时同时填 Gist ID + Token 即可双向，store.js 每次保存触发 onDataChanged → 防抖推送。
   涉及文件：js/cloud-sync.js
21. ✅ 大任务子任务在象限中的内容分行显示：
   (1) createTaskElement / createSubTaskElement 对带 bigTaskRef 的元素加 is-bigtask-sub 标记 class。
   (2) 桌面端 .is-bigtask-sub .task-text 改用 white-space:pre-wrap + word-break:break-word，
       保留用户输入的换行符并自动断行，长内容完整显示（不再被 nowrap+ellipsis 截断成一行）。
   (3) 大任务子任务左侧加 accent 色条，便于识别归属。
   (4) 手机端 .task-text 统一改用 pre-wrap，换行符保留。
   涉及文件：js/render.js / css/style.css
22. ✅ 大任务/小任务/阶段分行模式（仅手机端，桌面端保留紧凑原设计）：
   (1) createSubTaskElement / createStageElement / createStageElementForTask 给 checkbox+文字
       包进 <div class="task-left"> 容器，与 createTaskElement 结构统一（拖拽/事件/CSS 均不依赖
       直接子结构，加容器零影响——已用 3 个时间视图回归测试验证）。桌面端 task-left 默认 flex:1
       保持紧凑单行；该容器只为手机端分行提供锚点。
   (2) 手机端（@media ≤600px）：.subtask-item / .subtask-stage-item / .task-item flex-wrap，
       task-left 独占第一行（checkbox+文字 pre-wrap 不定多行，虚线分隔），操作图标按钮折第二行均分。
       三类条目（小任务/阶段/大任务子任务）布局完全一致。
   (3) 桌面端：subtask-item / subtask-stage-item 维持原紧凑单行设计；仅大任务子任务(is-bigtask-sub)
       的文字 pre-wrap 完整显示 + 左侧色条（便于识别归属），不强制折行。
   涉及文件：js/render.js / css/style.css
   验证：test/test-timeview-columns.js / test-timeview-distribution.js / test-timeview-grouping.js 全通过
23. ✅ 修复添加新任务时编辑界面跳转到已完成任务的 bug：
   addTask 原先取 DOM 最后一个 .task-text 进入编辑，但渲染排序为「未完成在前、已完成在后」，
   新任务(未完成)排在中间，DOM 最后一个反而是已完成的旧任务——导致编辑框跳错对象。
   改用 task.id 精确定位新任务的 task-text（data-id 选择器），并 scrollIntoView 滚到可视区。
   涉及文件：js/quadrant-ops.js
24. ✅ 云同步状态改为图标指示（替代弹窗通知）：
   (1) 右上角同步按钮(btnCloudSync)兼任状态指示器：空闲时显示 ☁️，同步过程中变成 ⏳ 沙漏，
       完成后恢复 ☁️ 云；失败时短暂显示 ⚠️（红色，3秒后自动恢复）。
   (2) pushToGist / pullFromGist / autoExportToDisk 的推送/拉取/写入过程中调用 setSyncIcon('syncing')，
       完成后调用 setSyncIcon('idle')，失败调用 setSyncIcon('error')——原先的成功 toast/alert 通知全部移除，
       仅保留错误时的 alert（必须告知用户）。
   (3) 对话框内推送按钮的独立 loading 状态保留（⏳ 诊断中... / ⏳ 推送中...），
       与右上角全局图标互不冲突。
   (4) 自动静默拉取(autoPullFromGist)不改图标，避免页面加载时打扰用户。
   涉及文件：js/cloud-sync.js / service-worker.js

25. ✅ 待办池任务添加勾选完成功能：
   (1) 数据结构：plan pool 任务和 block 子任务新增 completed: false 字段。
       新建入口（btnAddPlanTask / _addPlanSubtask）均已初始化，旧数据向后兼容（无 completed 字段默认为 false）。
   (2) UI：待办池/周计划/月计划三个 tab 的任务行新增 checkbox（planpool-checkbox，15px，accent-color）。
       勾选后行文字添加删除线 + opacity:0.55（与象限子任务完成态一致）。
   (3) 事件：checkbox change 通过 cfg.updateFn(ftId, {completed}) / cfg.editSubFn(ftId, stId, 'completed') 保存。
   (4) 日报：markdown 和 HTML 输出不再硬编码 ⬜，改为根据 completed 字段显示 ✅/⬜。
   涉及文件：js/render.js / js/future.js / js/daily-report.js / css/style.css / service-worker.js

26. ✅ 待办池任务支持划分阶段：
   (1) 数据函数：新增 _splitPlanTaskStages / _addPlanTaskStage / _deletePlanTaskStage /
       _togglePlanTaskStageComplete / _updatePlanTaskStageText 五个通用函数，参数化 poolKey+saveFn，
       与现有 plan pool 子任务函数风格一致。
   (2) UI：任务行新增 📋 划分阶段按钮（split-stages-btn，无阶段时显示）；点击后弹出逗号分隔的阶段名输入框，
       至少2个阶段名。阶段渲染为 planpool-stages 容器内的 planpool-stage-item 行（checkbox+文字+删除按钮），
       底部有 + 阶段 按钮。有阶段时父 checkbox 禁用（自动派生：全阶段完成→父任务完成）。
   (3) 事件：6 个新 selector 块（split-btn / stage-checkbox / stage-text dblclick / stage-del / add-stage），
       全部通过 cfg.poolKey+cfg.saveFn 参数化，三个池共享同一套逻辑。
   (4) PLAN_POOL_CONFIGS 新增 saveFn 字段，方便阶段操作统一引用。
   (5) 样式：planpool-stages / planpool-stage-item（虚线分隔）/ planpool-stage-checkbox /
       planpool-stage-text（完成态删除线）/ planpool-add-stage-btn，暗色模式自动适配（CSS变量），
       移动端触摸目标 ≥36px。
   涉及文件：js/render.js / css/style.css / service-worker.js
27. ✅ 任务池 block 子任务支持划分阶段：
   (1) 数据函数：新增 _splitPlanBlockSubStages / _addPlanBlockSubStage / _deletePlanBlockSubStage /
       _togglePlanBlockSubStageComplete / _updatePlanBlockSubStageText 五个函数，导航到
       tasks[i].tasks[j].stages，与 block 子任务数据结构匹配。
   (2) UI：每个 block 子任务行新增 📋 划分阶段按钮（无阶段时显示），阶段渲染为 planpool-block-stages
       容器（18px 额外缩进）。有阶段时父 checkbox 禁用，自动派生（全阶段完成→子任务完成）。
   (3) 事件：5 个 stage 事件块（split/checkbox/text/del/add）全部通过 data-st-id 分流——存在时
       调用 block 子任务版本函数，否则调用顶层任务版本。复用现有 CSS 类名、事件选择器。
   (4) 样式：planpool-block-stages（padding-left:18px）确保阶段行与子任务文本视觉对齐。
   涉及文件：js/render.js / css/style.css / service-worker.js
28. ✅ 新增 Gitee Gist 云同步 + 移除百度网盘同步：
   (1) 新增 Gitee Gist（码云代码片段）云同步：仿照 GitHub Gist 实现，支持公开代码片段免 Token 拉取、
       Token 推送双向同步、自动拉取、防抖推送、连接诊断。
      Gitee API：GET/PATCH gitee.com/api/v5/gists/{id}，Token 通过 access_token 参数传递。
   (2) 移除百度网盘同步功能：删除 setupBaiduSync / autoExportToDisk / importFromBaiduDisk 函数，
       IndexedDB 文件句柄持久化（openDB / storeFileHandle / restoreFileHandle），相关常量。
       localStorage 中旧 'baidu-disk' 模式自动迁移为禁用状态。
   (3) GitHub Gist 模式名从 'gist' 统一改为 'github-gist'，向后兼容自动迁移。
   (4) 同步设置对话框(openSyncSettings)重构：分别展示 GitHub Gist 和 Gitee Gist 两个配置区，
       支持 ID+Token 表单连接、推送/拉取按钮、禁用同步。
   (5) updateSyncIndicator / disableSync 适配双模式，同步图标 title 显示具体平台名称。
   (6) SW 缓存版本升至 v15。
   涉及文件：js/cloud-sync.js / service-worker.js
29. ✅ 依循项下新增"最近优先解决的问题"模块：
   (1) 数据模型：principles 对象新增 priorityProblems 数组（{id, text}），最多2条（建议1条）。
       向后兼容旧数据（自动初始化空数组）。
   (2) 存储：新增 addPriorityProblem / updatePriorityProblem / deletePriorityProblem。
   (3) UI：在原则列表和"+ 添加原则"按钮之间渲染红色主题分隔线和优先问题列表。
       双击编辑文字、×删除，满2条自动隐藏添加按钮。
   涉及文件：js/store.js / js/render.js / js/app.js / index.html / css/style.css
30. ✅ 大任务和小任务划分阶段支持可折叠功能：
   (1) 象限视图的任务/子任务有阶段时，右侧新增 ▼/▶ 折叠切换按钮。
   (2) 折叠状态持久化到 localStorage（quadrant_stages_collapsed），刷新页面保持。
   (3) 折叠时隐藏 .subtask-stages 容器和"+ 阶段"按钮。
   (4) 默认不折叠（▼ 展开态），仅象限视图生效（时间视图阶段分散分布，无需折叠）。
   涉及文件：js/store.js / js/render.js / css/style.css
31. ✅ 计划池任务和大任务加"立刻导入"选项：
   (1) 计划池任务（待办/周/月）：无阶段任务行新增 📥 导入按钮，含阶段任务在阶段容器底部
       新增"📥 导入全部阶段到今日"按钮；块内子任务同理。
   (2) 大任务含阶段子任务：原来有阶段时隐藏 📥 导入按钮（仅显示逐阶段导入），
       现在新增 📥"导入全部"按钮（复用 importBigSubtaskToToday，已支持 copyBigSubtaskStages）。
   (3) 导入语义：复制到今日 Q-II，不删除源数据。导入任务附带 planPoolRef 追溯字段。
       新增 importPlanPoolItemToToday / importPlanPoolItemWithStagesToToday。
   (4) SW 缓存版本升至 v16。
   涉及文件：js/render.js / js/future.js / js/bigtask.js / css/style.css / service-worker.js
32. ✅ 移除 Gitee Gist 云同步（仅保留 GitHub Gist）：
   (1) 删除 Gitee 全部同步代码：常量（GITEE_GIST_ID_KEY / GITEE_GIST_TOKEN_KEY）、
       syncInfo 的 giteeGistId / giteeGistToken 字段、init 中的 Gitee 自动拉取、
       onDataChanged 的 Gitee 分支、8 个 Gitee 函数（setupGiteeGist / saveGiteeGistConfig /
       fetchGiteeGist / diagnoseGiteeGistConnection / pushToGiteeGist / pullFromGiteeGist /
       autoPullFromGiteeGist / debouncePushToGiteeGist）、公开 API 的 Gitee 导出。
   (2) openSyncSettings 对话框移除 Gitee Gist 配置区，仅保留 GitHub Gist。
   (3) updateSyncIndicator / disableSync 移除 Gitee 分支。
   (4) 向后兼容：init 检测旧 'gitee-gist' 模式自动清空配置（含 localStorage 残留的
       cloudsync_gitee_gist_id / cloudsync_gitee_gist_token），降级为未启用状态。
   (5) CODE_VERSION 更新为 '2026-07-11-remove-gitee'，SW 缓存版本升至 v17。
   涉及文件：js/cloud-sync.js / service-worker.js
33. ✅ 修复优先问题模块空状态下无法添加的 bug：
   renderPrinciplesPanel 中，当 priorityProblems 为空时把 ppActions
   （含"+ 添加优先问题"按钮）一并隐藏，导致用户永远无法添加第一条——
   功能看起来"不存在"。改为空状态仅隐藏分隔线，保留添加按钮可见。
   SW 缓存版本升至 v18。
   涉及文件：js/render.js / service-worker.js
34. ✅ 大任务卡片新增"一键导入今日任务池"按钮（📥，卡片标题栏可见，无需展开）：
   (1) 痛点：原大任务导入入口埋在 卡片→里程碑→子任务 三层折叠里，极难发现；
        且与计划池（平铺、显眼）体验不一致。
   (2) 新增 future.js planBigTaskTodayImport（纯函数，去重逻辑同 migrateBigTaskSubtasks，
        但限定单个大任务）+ importBigTaskTodayPoolToToday：把该大任务"今日任务池"
        （plannedDate=当前查看日期 且未完成）的全部子任务一键导入今日 Q-II，
        已导入的自动跳过（跨 I/II/III/IV 象限及块内子任务均去重），含阶段一并深拷贝。
   (3) bigtask.js renderBigTaskCardHTML 头部按 todayPoolCount>0 条件渲染 📥 按钮
        （标题"一键导入今日任务池（N 条）"）；renderBigTaskPanel 绑定 .bt-import-today-btn。
   (4) 新增回归测试 test/test-bigtask-import-today.js（6 组用例：空数据/去重/跨象限/
        块子任务/跨大任务不误删/空池），全部通过。
   (5) SW 缓存版本升至 v19。
   涉及文件：js/future.js / js/bigtask.js / service-worker.js / test/test-bigtask-import-today.js

（暂无）

60. ✅ 计划池阶段可折叠：为计划池任务（_renderPlanTaskHTML）和块内子任务（_renderPlanBlockHTML）
    的阶段容器新增 ▼/▶ 折叠切换按钮，复用 store.js 的 setStageCollapsed 持久化到
    localStorage（quadrant_stages_collapsed，用 pp-stages- 前缀避免与象限视图冲突）。
    默认展开（▼），点击折叠后刷新页面保持状态。
61. ✅ 计划池任务块批量导入：新增 importPlanBlockAllToToday（js/future.js），
    在块头部新增 📥 一键导入全部子任务按钮（仅当有未完成子任务时显示），
    去重依据 planPoolRef（跨象限 + 块内子任务），含阶段的子任务一并深拷贝阶段。
    SW 缓存版本升至 v21。
    涉及文件：js/render.js / js/future.js / service-worker.js
62. ✅ 批量删除功能：新增 js/batch-delete.js，支持在象限/计划池/大任务三大区域
    进入批次模式（📋批删按钮），多选条目后一键批量删除。
    - 进入批次模式后每行左侧出现选择复选框，底部出现操作栏（全选/反选/删除/取消）
    - 象限视图：支持批量删除任务、任务块、块内子任务、阶段（含 bigTaskRef 解绑）
    - 计划池：支持批量删除待办/周/月池的任务、任务块、块内子任务、阶段
    - 大任务面板：支持批量删除卡片、里程碑、子任务、阶段
    - 切换视图/面板/日期自动退出批次模式
    SW 缓存版本升至 v22。
    涉及文件：js/batch-delete.js / js/render.js / js/bigtask.js / js/future.js
              / js/app.js / index.html / css/style.css / service-worker.js

────────────────────────────────────────

【同步可靠性深度重构 + 性能优化 — 2026-07-11】

── 同步数据丢失缺陷修复 ──
35. ✅ 计划池 localStorage key 错配修复：cloud-sync.js 导出/导入计划池键从错误的
    "quadrant_pool_*" 改为正确的 "quadrant_future_tasks"/"quadrant_week_tasks"/"quadrant_month_tasks"
36. ✅ 大任务缓存库 key 单复数错配修复：cloud-sync.js 导入大任务缓存从单数 "quadrant_big_task_cache"
    改为复数 "quadrant_big_tasks_cache"
37. ✅ 移除诊断测试 PATCH 毒化云端：diagnoseGistConnection 不再用测试 payload 覆盖 Gist，
    改为直接真实推送
38. ✅ 修复启动时静默 autoPull 覆盖本地未推送修改：setupGistSync 初始化不再自动拉取，
    改为 toast 提示手动拉取
39. ✅ onDataChanged 遗漏修复：saveBigTasks/saveBigTaskCache/savePlanTasks/savePrinciples
    末尾统一调用 CloudSync.onDataChanged()

── 可靠合并同步引擎 ──
40. ✅ 新建 js/sync-merge.js：逐条目 LWW（最后写赢）合并引擎，纯函数、node 可测
    - lwwWinner：按 updatedAt 比较，墓碑优先，旧数据向后兼容
    - mergeArrayById：按 ID 合并两个数组，支持递归子条目
    - mergeDateData / mergeAllDateData：象限/多日数据合并
    - mergeFlatArray / mergePrinciples / mergeCachedDatesIndex
    - countRemoteChanges：远端变更统计
    - 验证：test/test-cloudsync-merge.js（25 测试全通过）
41. ✅ 拉取流程改为非破坏性合并：pullFromGist 使用 mergeImportData 逐集合合并
    （替代原 importAllData 全量覆盖），显示合并统计 toast
42. ✅ 推送流程改为推前合并：pushToGist 先 GET 远程、mergeImportData 合并到本地、
    再 PATCH 合并后数据（推前合并不覆盖远程改动）
43. ✅ 竞态控制：_syncInFlight 锁 + _pendingPush 排队，推送完成后自动 flush 排队请求
44. ✅ 推送成功清除 lastLocalEdit 标记

── 性能优化（渲染与存储）──
45. ✅ store.js 内存缓存层：_allDataCache + _fastDeepClone，loadAllData 命中缓存时
    零 JSON.parse 开销；saveAllData 自动更新缓存
46. ✅ 备份轮转限频：_rotateRollingBackups 限频 30s，高频小操作跳过备份轮转
47. ✅ 统计合并遍历：新增 calcAllStats 函数，一次 walkLeafItems 同时算出象限/加权/时段
    三组指标，updateStatsBar 从 2 次遍历减为 1 次
48. ✅ renderTimeView 去重数据加载：renderAll 将已加载 data 传给 renderTimeView，
    避免重复 loadDateData（减少一次完整 JSON.parse）
49. ✅ migrate 短路：_migratedToday 按日期缓存，同一天多次 renderAll 只执行一次迁移
50. ✅ renderAll debounce：renderAllDebounced 50ms 防抖，连续操作合并为一次渲染
51. ✅ SourceEditor 延迟初始化：init 改为首次 open 时调用，避免启动时捕获完整 DOM
52. ✅ 启动任务延迟：migrateFutureTasks/WeekTasks/MonthTasks 改为 setTimeout(0) 延迟执行，
    seedCacheIndexIfEmpty 同步延迟；大任务自动归档改为 one-time migration
    （quadrant_bigtask_autoarchive_v1 标记）
53. ✅ 消除启动时双重面板渲染：renderAll 已内调 renderBigTaskPanel/renderPlanPoolPanel/
    renderPrinciplesPanel，移除 initApp 中的冗余调用

── Service Worker 与缓存策略 ──
54. ✅ SW 缓存版本升级至 v20
55. ✅ 新增 js/sync-merge.js 到 PRECACHE_ASSETS（原遗漏）
56. ✅ fetchAndCache 修复：cache.put 前 clone 响应，避免 body stream 被消费两次
57. ✅ 后台更新扩展至 JS/CSS：SW 对 HTML/JS/CSS 资源都执行 Stale-While-Revalidate
58. ✅ index.html 所有 <script> 加 defer 属性 + ?v=20 缓存刷新参数；
    <link> CSS 同样加 ?v=20

── 回归测试 ──
59. ✅ 全部 8 个测试套件（100+ 测试）通过：
    test-cloudsync-merge.js / test-cloudsync-store-keys.js /
    test-timeview-columns.js / test-timeview-distribution.js /
    test-timeview-grouping.js / test-bigtask-completion.js /
    test-bigtask-import-today.js / test-bigtask-cache.js

涉及文件：js/cloud-sync.js / js/sync-merge.js（新增）/ js/store.js / js/render.js /
    js/app.js / js/source-editor.js / service-worker.js / index.html /
    test/test-cloudsync-merge.js（新增）/ test/test-cloudsync-store-keys.js（新增）

63. ✅ 缓存系统增强——回收站机制：大任务/依循/计划池的删除和完成操作
    不再永久删除，而是移入回收站缓存，支持置顶保护和恢复。

    【导入缓存UI升级】
    - 缓存日期索引从 string[] 升级为 [{date, label}][]，向后兼容自动迁移
    - 导入缓存界面支持重命名：双击名称或点击 ✏️ 按钮自定义标签（如"工作日常用"）
    - 每条缓存增加 📤 导出JSON 按钮，可将单日数据导出为 tasks-xxx.json
    - 新增 getCachedDateEntries() / updateCachedDateLabel() 函数

    【大任务回收站】
    - 删除大任务/里程碑/子任务/阶段 → 移入 `quadrant_big_tasks_deleted`（上限10条）
    - 新增 `_extractAndCacheBigTaskItem()` 通用提取函数，自动处理父信息记录
    - 大任务面板底部新增"🗑️ 大任务回收站"可折叠区域
    - 每条缓存支持：📌 置顶保护 / ↩ 恢复到原位置 / 🗑 永久删除
    - 删除大任务整卡时正确跳过 recalcBigTaskProgress（修复 splice 后索引错位bug）

    【依循回收站】
    - 删除原则/优先问题 → 移入 `quadrant_principles_deleted`（上限10条）
    - 依循面板底部新增"🗑️ 依循回收站"可折叠区域
    - 同样支持 📌 置顶 / ↩ 恢复 / 🗑 永久删除

    【计划池回收站】
    - 待办/周/月三个池各自独立的缓存键：`quadrant_future_tasks_cache` 等（各上限10条）
    - 完成时自动移入：勾选checkbox → 从池中移除 → 缓存区出现（action: "completed"）
    - 阶段全部完成也自动触发父任务移入缓存
    - 删除时自动移入：所有 delete 函数改造为 move-to-cache（action: "deleted"）
    - 计划池面板底部新增"🗑️ 计划池回收站"可折叠区域（按Tab切换显示对应池缓存）
    - 同样支持 📌 置顶保护 / ↩ 恢复到池中（清除completed状态）/ 🗑 永久删除

    【通用缓存引擎】
    - 新增通用函数：addToCache / removeFromCache / toggleCachePin / getCacheEntries
    - 缓存满（10条）自动驱逐最旧的 unpinned 条目；全置顶时拒绝添加并提示
    - 所有缓存条目带 timestamp 和 pinned 字段

    涉及文件：js/store.js（核心重构） / js/bigtask.js / js/render.js / js/cache-ui.js /
        css/style.css / index.html / service-worker.js

64. ✅ 回收站增强——依循回收站分离、默认收起、详情查看、格式统一

    【原则与优先问题缓存分离】
    - 新增 `quadrant_priority_problems_deleted` 键，优先问题独立缓存
    - `_extractAndCachePrinciple` 按类型分别存入不同缓存键
    - 新增 `restorePriorityProblemFromDeletedCache()` 恢复函数
    - HTML 新增独立"🗑️ 优先问题回收站"区域
    - 渲染拆分为 `renderPrinciplesDeletedCache` + `renderPriorityProblemsDeletedCache`

    【回收站默认收起】
    - 所有回收站（大任务/原则/优先问题/计划池）默认折叠，点击标题栏展开
    - 折叠状态持久化到 `quadrant_cache_toggle_state`
    - 新增通用函数：`setupCacheSectionToggle(sectionId)` 处理展开/收起
    - 标题图标 ▶/▼ 随状态切换

    【缓存内容可展开查看】
    - 每条缓存增加 📋 详情按钮，点击弹出 Markdown 风格详情面板
    - 新增 `renderCacheDetailMarkdown(entry)` 生成结构化文本
    - 新增 `_showCacheDetailPopup(entry)` 弹出模态窗口
    - 大任务/里程碑/子任务/阶段/任务块 均支持完整信息展示（含阶段进度、日期等）
    - 新增 `_renderGenericDeletedCacheList()` 统一渲染逻辑（含展开/置顶/恢复/删除）

    【大任务缓存格式统一】
    - 大任务缓存条目格式改为与计划池一致：`{action, timestamp}` 替代 `deletedAt`
    - 四项缓存条目（bigtask/milestone/subtask/stage）全部更新

    涉及文件：js/store.js / js/bigtask.js / js/render.js / css/style.css / index.html

65. ✅ 导入缓存增强——支持删除缓存条目和置顶保护

    【导入缓存删除+置顶】
    - `quadrant_cached_dates_index` 新增 `pinned` 字段，向后兼容自动迁移
    - 新增 `removeCachedDate(date)` — 从导入索引中移除指定日期（不删除实际数据）
    - 新增 `toggleCachedDatePin(date)` — 切换缓存条目的置顶状态（点击即切换）
    - 导入缓存列表：置顶条目排在最前面 + 📌 图标，非置顶显示 📍
    - 每条缓存增加 📌 置顶按钮和 🗑️ 删除按钮
    - 删除缓存仅移除索引条目，不影响 localStorage 中实际日期数据

    涉及文件：js/store.js / js/cache-ui.js

66. ✅ 同日期支持多条缓存快照——每次缓存生成独立条目

    【同日期多次缓存】
    - `quadrant_cached_dates_index` 条目新增 `id` 和 `cachedAt` 字段（V3），向后兼容自动迁移
    - `markDateAsCached(date)` 不再检查重复，每次调用都创建新条目并分配唯一ID
    - `getCachedDates()` 返回去重后的唯一日期列表，保证导入等逻辑不受影响
    - `updateCachedDateLabel(id)` / `removeCachedDate(id)` / `toggleCachedDatePin(id)` 改用ID操作
    - `seedCacheIndexIfEmpty()` 也生成 `id` + `cachedAt` 字段
    - `cache-ui.js` 中所有操作（置顶/删除/重命名）使用 `entry.id` 而非 `entry.date`

    涉及文件：js/store.js / js/cache-ui.js


────────────────────────────────────────

67. ✅ 置顶缓存支持按星期自动导入——工作日/周六/周日三个独立开关

    【自动导入机制】
    - `quadrant_cached_dates_index` 条目新增 `autoWorkday`/`autoSaturday`/`autoSunday` 字段（V4）
    - 向后兼容自动迁移，旧条目缺失字段默认 false
    - 新增 `setCachedDateAutoImport(id, field, value)` — 设置单条目的自动导入选项
    - 新增 `getAutoImportEntriesForDate(dateStr)` — 按星期筛选匹配的置顶条目
    - 新增 `silentImportCachedData(sourceDate, targetDate)` — 静默合并（无alert/confirm）
    - 新增 `runAutoImportsForDate(dateStr)` — 对指定日期执行全部匹配的自动导入
    - `renderAll()` 开头调用 `runAutoImportsForDate()`，数据变更后重新加载渲染

    【导入缓存UI】
    - 置顶条目下方新增"⏰ 自动导入"行，含三个切换按钮：工作日 / 周六 / 周日
    - 激活的按钮高亮显示（btn-primary样式），点击即切换开关状态
    - 仅置顶条目显示自动导入选项（非置顶不显示）

    【星期映射】
    - 工作日：周一至周五（getDay 1-5）
    - 周六：getDay 6
    - 周日：getDay 0

    涉及文件：js/store.js / js/cache-ui.js / js/render.js / service-worker.js

68. ✅ 导入缓存界面重构——两行紧凑布局 + 象限数量徽标

    【布局重构】
    - Row 1：名称（左）+ 四象限任务数量徽标（右），space-between 对齐
    - Row 2：日期提示（仅自定义名称时显示）+ 全部操作按钮 + 自动导入开关
    - 卡片改为 column 方向布局，整体更紧凑美观
    - 象限徽标按 I/II/III/IV 配色：红/蓝/金/绿，仅非零时着色
    - 暗色模式：徽标适配深色背景，降低亮度避免刺眼
    - 移动端：徽标和按钮缩小适配窄屏

    涉及文件：js/cache-ui.js / css/style.css / service-worker.js

69. ✅ 修复待办池任务加入象限后删除/完成/推迟失效——4项根因修复

    【根因1：自动导入重复触发（CRITICAL）】
    - `runAutoImportsForDate` 在每次 `renderAll` 中运行，导致用户删除任务后又被重新导入
    - 新增 `quadrant_auto_import_tracker` 追踪机制：每个(entryId, targetDate)对仅自动导入一次
    - 新增 `_wasAutoImported` / `_markAutoImported` 辅助函数

    【根因2：_migratePlanPool 缺少 timeSlot】
    - 简单任务迁移路径：缺少 `timeSlot` → 新增 `timeSlot: getDefaultTimeSlot()`
    - 块内子任务提取路径：缺少 `timeSlot` → 新增 `timeSlot: st.timeSlot || getDefaultTimeSlot()`
    - 块迁移子任务：改用 `st.timeSlot || getDefaultTimeSlot()` 优先保留原时段
    - 块迁移子任务：补充缺失的 `progress: '100%'`

    【根因3：_migratePlanPool 丢失 bigTaskRef】
    - 简单任务 + 块内子任务迁移时，保留 pool 条目的 `bigTaskRef`（若存在）
    - 确保推迟回池后再迁移的任务仍能关联大任务，删除/完成可正确同步

    【根因4：deferQuadrantTask 丢失 bigTaskRef】
    - 推迟象限任务到待办池时，`bigTaskRef` 未被保留
    - 修复后池中任务携带 `bigTaskRef`，再迁移回象限时保持关联

    涉及文件：js/store.js / service-worker.js

70. ✅ 修复依循回收站消失 + 移动端JSON粘贴导入

    【依循回收站恢复】
    - `renderPrinciplesPanel` 中3处早期 `return` 导致回收站渲染代码不可达
    - 原则为空时 return（line 2440）、ppListEl 缺失时 return（line 2481）、
      优先问题为空时 return（line 2493）——均跳过末尾的 recycle bin 渲染
    - 重构为条件块结构：active items 渲染与 recycle bin 渲染解耦，
      回收站在函数末尾始终执行，无论是否有活跃条目

    【移动端JSON导入增强】
    - 原先导入仅支持文件选择器（`<input type="file">`），移动端体验差
    - 新增 `showJsonImportModal()`：同时支持 📁 选择文件 + 📋 粘贴JSON
    - 粘贴模式提供"合并导入"和"覆盖导入"两个按钮
    - 电脑端导出JSON → 复制文本 → 手机端粘贴导入，无需文件传输
    - 模态框适配移动端：全宽按钮、大触摸目标

    涉及文件：js/render.js / js/json-io.js / service-worker.js

71. ✅ 彻底修复依循回收站消失 + 手机端导入电脑端数据

    【根因：资源缓存版本号未同步（关键）】
    - 上一轮（#70）改了 js/render.js、js/json-io.js 的代码内容，
      但 index.html 的 `?v=23` 资源版本号未升级
    - SW 用 Stale-While-Revalidate，cache key 含 `?v=23`，
      持续返回修复前的旧代码 → 用户设备上回收站仍不显示、导入仍只有文件选择器
    - 修复：index.html 所有资源 `?v=23` → `?v=29`（1 CSS + 24 JS）
    - SW `CACHE_VERSION` v28 → v29，激活时清空旧缓存
    - 双管齐下确保用户刷新后必拿到 #70 的修复代码

    【跨设备导入闭环增强】
    - #70 已实现手机端粘贴导入，但电脑端导出只下载文件，缺复制文本入口
    - 新增 `showJsonExportModal(jsonText)`：导出时弹出模态框
      含只读 textarea 显示完整 JSON + "📋 复制全部"按钮
    - 复制兼容性：优先 execCommand('copy')，降级 navigator.clipboard
    - `exportAllDataAsJSON` 浏览器分支下载后调用该模态框
    - 完整流程：电脑端导出 → 复制文本 → 发到手机 → 手机端导入JSON → 粘贴

    【验证】
    - 新增 test/test-json-io.js：覆盖 merge 保留旧日期/overwrite 全覆盖/
      非法JSON拒绝/非对象拒绝/不污染原数据/空库导入 6 组场景
    - 全部 9 套测试通过（含新增 1 套）

    【说明】
    - 若手机端是 Capacitor 打包的 App（非浏览器 PWA），SW 缓存机制不适用，
      需重新打包 App 才能更新代码；浏览器访问的 PWA 本次修复直接生效
    - 首次刷新时 SW 会自动检测新版本并在 3 秒后 reload（index.html 已有逻辑）

    涉及文件：index.html / service-worker.js / js/json-io.js / js/store.js /
        test/test-json-io.js（新增）

72. ✅ 修复手机端无法同步电脑端/云端数据 — 3项根因修复

    【Bug 1：Gist 连接成功后不自动拉取数据（最直接影响手机端）】
    - `setupGistSync` 连接 Gist 后只保存配置 + 提示"点击拉取"
    - 用户输入 Gist ID 点连接后，数据并未实际下载，界面仍为空
    - 直到下次启动 App 时 `autoPullFromGist` 才会拉取
    - 手机端新用户连上 Gist 后看到空界面，不知还需手动点"拉取"
    - 修复：新增 `onConnectSuccess` 辅助函数，连接成功后立即调
      `pullFromGist()` 自动拉取数据并刷新界面

    【Bug 2：Capacitor 导出文件名与 autoSyncFromDevice 不匹配】
    - `exportAllDataAsJSON` 导出到 Documents/quadrant_tasks_backup_YYYY-MM-DD.json
    - `autoSyncFromDevice` 读取 Documents/quadrant_tasks_backup.json（固定名）
    - 两者文件名永远对不上 → autoSyncFromDevice 永远读不到导出文件
    - 修复：导出时同时写入 dated 文件名和 SYNC_FILENAME 固定名，
      确保 autoSyncFromDevice 在下次启动时能检测到备份

    【Bug 3：Capacitor 导出不显示可复制 JSON 弹窗】
    - `showJsonExportModal(json)` 调用仅在浏览器分支（else），
      Capacitor 分支导出后不显示可复制文本弹窗
    - 手机端导出后无法复制 JSON 文本发送给另一设备
    - 修复：将 showJsonExportModal 调用移到 if/else 两个分支都执行

    【缓存版本同步】
    - SW CACHE_VERSION v29 → v30
    - index.html 所有资源 ?v=29 → ?v=30

    【测试验证】
    - test/test-cloudsync-merge.js：25/25 通过
    - test/test-cloudsync-store-keys.js：通过
    - test/test-json-io.js：全部通过

    涉及文件：js/cloud-sync.js / js/store.js / service-worker.js / index.html

────────────────────────────────────────

【V1.6 云同步修复，已修正】
29. ✅ Gist 云同步 4 项修复——诊断覆盖正式数据 + 静默失败无感知 + 无重试 + SW缓存过期：
   (1) Bug 1（致命）：pushToGist() 诊断阶段 diagnoseGistConnection 将测试数据
       {_test:true} 写入正式文件，若后续推送失败 Gist 残留无效数据。
       修复：诊断改用独立测试文件 _sync_test_.json，推送成功后清理。
   (2) Bug 2（致命）：autoPullFromGist 静默模式错误仅 console.error，手机端无感知。
       修复：静默模式也闪烁 ⚠️ 图标 3 秒；importAllData 无效数据返回 false。
   (3) Bug 3（中等）：自动拉取无重试机制。
       修复：_autoPullWithRetry 最多重试 2 次，指数退避 2s→4s。
   (4) SW 缓存版本升 v15→v16。
   涉及文件：js/cloud-sync.js / service-worker.js

30. ✅ 同步拉取改为合并模式 + 桌面端定时拉取（手机→电脑方向修复）：
   (1) 拉取合并策略：importAllData() 从覆盖改为按 ID 合并（本地优先，云端补充）。
       涵盖 dateData/计划池/大任务活跃列表+缓存库。
   (2) 新增定时自动拉取：每 60 秒静默 pullFromGist(true)。
       页面不可见时暂停，恢复可见时立即拉取。
   (3) SW 缓存版本升 v16→v17。
   涉及文件：js/cloud-sync.js / service-worker.js

31. ✅ 修复 SW 缓存 Gist API 响应导致永远返回旧数据（GitHub Pages 同步根因）：
   (1) Bug（致命）：SW Cache First 策略拦截 api.github.com GET 请求，
       首次成功后缓存 API 响应，后续永远返回旧数据。
       Stale-While-Revalidate 仅在 text/html 时触发，API 请求永不刷新。
       这是 GitHub Pages 站点无法同步的根因。
       修复：api.github.com 直接 bypass SW 走网络。
   (2) 双重保险：所有 Gist GET 请求添加 cache: 'no-cache'。
   (3) SW 缓存版本升 v17→v18。
   涉及文件：service-worker.js / js/cloud-sync.js

32. ✅ 代码审查修复（10项）——重构 batch-delete 回收站 + sync-merge 递归合并等：
    (1) 恢复函数父级检查：restoreBigTaskFromDeletedCache / restorePlanPoolFromCache
        先检查父任务/父池是否存在再移除缓存，避免资源丢失。
        涉及文件：js/store.js
    (2) batchExecuteDelete 接入回收站：大任务删除改用 _extractAndCacheBigTaskItem
        自动走回收站 + recalcBigTaskProgress；计划池删除改用 _extractAndCachePlanPoolItem
        自动走回收站；阶段级删除手动 addToCache。
        涉及文件：js/batch-delete.js
    (3) sync-merge 递归子条目合并：mergeArrayById 中 block.tasks / item.stages /
        item.milestones 三个子数组递归调用 mergeArrayById 合并，空桩替换为实际递归。
        涉及文件：js/sync-merge.js
    (4) _migratedToday 日期切换清除：切日期时重置 _migratedToday 防止新日期跳过迁移。
        涉及文件：js/app.js
    (5) 计划池复选框仅切换完成态（不移除）：勾选/取消勾选仅设 completed 字段，
        移除原先的 _extractAndCachePlanPoolItem 路径（该路径将任务移出池）。
        涉及文件：js/render.js
    (6) invalidateDataCache 注册 storage 事件监听：另一标签页修改 localStorage
        时自动失效内存缓存，无需手动刷新。
        涉及文件：js/store.js
    (7) silentImportCachedData 去重：重构为委托 importCachedData(date, targetDate, silent)，
        消除 70+ 行重复逻辑。
        涉及文件：js/store.js
    (8) 四函数 save/cache 顺序修正：_extractAndCachePlanPoolItem /
        _mayBeCompleteAndCachePlanPoolItem / _extractAndCacheBigTaskItem /
        _extractAndCachePrinciple 改为 cache 成功后才 save，缓存满时不丢数据。
        涉及文件：js/store.js
    (9) 回收站渲染死代码清理：_renderGenericDeletedCacheList 中 restore 按钮
        去除无效的三元表达式 '已恢复' + (restored ? '' : '')。
        涉及文件：js/render.js
    (10) 移动端批次复选框 22→26px：触摸目标达 ≥26px 规范。
        涉及文件：css/style.css

────────────────────────────────────────

【日期缓存系统重构（快照架构）— 2026-07-12】

73. ✅ 日期缓存从引用模型重构为快照模型——15项修改覆盖 store/cache-ui/app/cloud-sync/sync-merge/recover-today：
   (1) 核心架构：缓存条目新增 snapshot 字段存储完整数据快照（JSON深拷贝），
      缓存不再是"书签"而是真正的数据存档。删除缓存条目不会丢失主数据，
      但快照独立于主数据，修改一个不影响另一个。
   (2) markDateAsCached(date) 重写：内部检查内容→深拷贝快照→存入索引条目，
      返回 boolean 供 UI 判断成功/失败。
   (3) 新增 importCachedDataFromSnapshot(entryId, targetDate, silent)：
      从快照读取数据合并到目标日期，ID 去重。
   (4) getCachedDateData(date) 重写：优先从快照读取，回退到主数据。
   (5) 新增 updateCacheSnapshot(entryId)：刷新已有缓存条目的快照为当前最新数据。
   (6) 新增 clearAllCachedDates()：清空全部缓存快照和自动导入追踪器。
   (7) 新增 removeOldDateData(daysToKeep)：清理超过N天且未被缓存引用的旧日期数据。
   (8) 新增 migrateCacheIndexToSnapshot()：一次性将旧格式（无snapshot）条目
      升级为快照格式，从主数据填充初始快照。通过迁移标记防重复执行。
   (9) loadCachedDatesIndex 新增 snapshot 字段向后兼容（缺失→null）。
   (10) seedCacheIndexIfEmpty() 改为空壳（no-op），不再自动填充缓存索引。
   (11) 删除重复的 silentImportCachedData 死代码（原来定义了两次）。
   (12) 删除 getAllCachedDates() 命名误导函数（读主数据而非缓存索引）。
   (13) 修复 cloud-sync.js + sync-merge.js mergeCachedDatesIndex 的 indexOf 对象比较 bug
       （引用相等永远匹配不上，导致每次同步都重复添加缓存条目，改用 ID 去重）。
   (14) 修复 recover-today.js 恢复逻辑：优先检查缓存快照条目，有数据直接恢复。
   (15) 移除 cleanup-cache.js（一次性脚本，已被完整缓存管理功能取代）。

   【缓存 UI 升级】
   - showCacheModal 重写：象限计数改用 entry.snapshot；新增快照预览（前5条任务名称）；
     新增🔄更新快照按钮；新增"全部清除"按钮（含确认对话框）；导入改用
     importCachedDataFromSnapshot，支持无快照条目提示用户先更新；
     导入后不关闭弹窗（支持连续导入多条）；导出JSON优先从快照读取；
     删除确认文案优化。
   - btnCacheSave 简化：markDateAsCached 内部已检查内容+创建快照，UI 仅判断返回值。

   涉及文件：js/store.js（核心重构） / js/cache-ui.js / js/app.js /
   js/cloud-sync.js / js/sync-merge.js / js/recover-today.js /
   index.html / service-worker.js

74. ✅ 任务块/多条目折叠功能——四象限/时间视图/计划池全部支持：
   (1) 四象限视图：createTaskBlockElement 中为有子任务的块在 header 内添加
       ▼/▶ 折叠按钮，可折叠 .block-tasks（子任务列表）+ .add-subtask-btn。
   (2) 时间视图：createParentHeaderEl 中为块级父标题添加折叠按钮，点击时
       遍历后续兄弟元素隐藏/显示所有缩进子元素（.timeview-indent-1/2/orphan-label）。
   (3) 计划池：_renderPlanBlockHTML 中在块 header 添加折叠按钮，
       .planpool-block-tasks 新增 id 便于定位；renderPlanPoolPanel 中新增
       .planpool-block-toggle 事件绑定，复用已有 planpool-stages-toggle 模式。
   (4) 持久化：全部复用现有 localStorage key（quadrant_stages_collapsed），
       使用命名空间前缀区分：block-{id}（象限块）、tv-parent-{slot}-{id}（时间视图）、
       pp-block-{id}（计划池块）。无需修改 store.js 或 CSS。
   (5) 回归测试：test-timeview-columns.js / test-timeview-distribution.js /
       test-timeview-grouping.js 全部通过。

   涉及文件：js/render.js

────────────────────────────────────────

【未来计划】
1. 代码重构：模块化拆分，提升可维护性和运行性能
2. APP与网页版数据互通
